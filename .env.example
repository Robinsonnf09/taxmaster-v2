# Database (obter no Railway)
DATABASE_URL=postgresql://usuario:senha@host:porta/database

# JWT Secret (gerar uma chave aleatória)
JWT_SECRET=taxmaster_secret_key_change_this

# Node Environment
NODE_ENV=production
