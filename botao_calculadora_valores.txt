
<!-- Adicionar este card na página processo_detalhe.html -->

<div class="card shadow-sm mb-4">
    <div class="card-header bg-success text-white">
        <h5 class="mb-0">
            <i class="fas fa-calculator me-2"></i>
            Atualização de Valores
        </h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-8">
                <p class="mb-2">
                    <strong>Valor Principal:</strong> 
                    R$ {{ "{:,.2f}".format(processo.valor_principal) if processo.valor_principal else "Não informado" }}
                </p>
                <p class="mb-2">
                    <strong>Juros Calculados:</strong> 
                    R$ {{ "{:,.2f}".format(processo.valor_juros) if processo.valor_juros else "0,00" }}
                </p>
                <p class="mb-2">
                    <strong>Correção Monetária:</strong> 
                    R$ {{ "{:,.2f}".format(processo.valor_correcao_monetaria) if processo.valor_correcao_monetaria else "0,00" }}
                </p>
                <p class="mb-2">
                    <strong>Valor Total Atualizado:</strong> 
                    <span class="text-success fs-5">
                        R$ {{ "{:,.2f}".format(processo.valor_atualizado) if processo.valor_atualizado else "0,00" }}
                    </span>
                </p>
                {% if processo.data_ultima_atualizacao_valor %}
                <p class="mb-0 text-muted">
                    <small>Última atualização: {{ processo.data_ultima_atualizacao_valor.strftime('%d/%m/%Y %H:%M') }}</small>
                </p>
                {% endif %}
            </div>
            <div class="col-md-4 text-end">
                <a href="/processo/{{ processo.id }}/atualizar-valores" 
                   class="btn btn-success btn-lg">
                    <i class="fas fa-calculator me-2"></i>
                    Calcular Valores
                </a>
            </div>
        </div>
    </div>
</div>

